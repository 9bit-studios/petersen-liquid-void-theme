{%- comment -%}
  Petersen Games - Core Game Hero Section
  Advanced tabbed hero section for core games with rich content support
  Designed differently from product details tabs with video and blog content support
{%- endcomment -%}

<div class="petersen-core-hero" id="section-{{ section.id }}">
  <div class="hero-container">
    {%- if section.settings.show_title -%}
      <div class="hero-header">
        <h2 class="hero-title">{{ section.settings.title | default: 'Explore the Game Universe' }}</h2>
        {%- if section.settings.subtitle != blank -%}
          <p class="hero-subtitle">{{ section.settings.subtitle }}</p>
        {%- endif -%}
      </div>
    {%- endif -%}

    <div class="hero-tabs-wrapper">
      {%- comment -%} Redesigned Apple Store-style Tab Navigation {%- endcomment -%}
      <div class="hero-tabs-nav" role="tablist">
        {%- if section.settings.show_gameplay -%}
          <button class="hero-tab-button active"
                  data-tab="gameplay"
                  role="tab"
                  aria-selected="true"
                  aria-controls="hero-tab-gameplay-{{ section.id }}"
                  id="hero-nav-gameplay-{{ section.id }}">
            {%- if section.settings.gameplay_icon != blank -%}
              <img src="{{ section.settings.gameplay_icon | image_url: width: section.settings.tab_icon_width }}" alt="" class="tab-glyph" style="width: {{ section.settings.tab_icon_width }}px; height: {{ section.settings.tab_icon_width }}px;">
            {%- endif -%}
            <span class="tab-label">{{ section.settings.gameplay_title | default: 'Gameplay' }}</span>
          </button>
        {%- endif -%}

        {%- if section.settings.show_downloads -%}
          <button class="hero-tab-button"
                  data-tab="downloads"
                  role="tab"
                  aria-selected="false"
                  aria-controls="hero-tab-downloads-{{ section.id }}"
                  id="hero-nav-downloads-{{ section.id }}">
            {%- if section.settings.downloads_icon != blank -%}
              <img src="{{ section.settings.downloads_icon | image_url: width: section.settings.tab_icon_width }}" alt="" class="tab-glyph" style="width: {{ section.settings.tab_icon_width }}px; height: {{ section.settings.tab_icon_width }}px;">
            {%- endif -%}
            <span class="tab-label">{{ section.settings.downloads_title | default: 'Downloads' }}</span>
          </button>
        {%- endif -%}

        {%- if section.settings.show_expansions -%}
          <button class="hero-tab-button"
                  data-tab="expansions"
                  role="tab"
                  aria-selected="false"
                  aria-controls="hero-tab-expansions-{{ section.id }}"
                  id="hero-nav-expansions-{{ section.id }}">
            {%- if section.settings.expansions_icon != blank -%}
              <img src="{{ section.settings.expansions_icon | image_url: width: section.settings.tab_icon_width }}" alt="" class="tab-glyph" style="width: {{ section.settings.tab_icon_width }}px; height: {{ section.settings.tab_icon_width }}px;">
            {%- endif -%}
            <span class="tab-label">{{ section.settings.expansions_title | default: 'Expansions' }}</span>
          </button>
        {%- endif -%}
      </div>

      {%- comment -%} Hero Tab Content Panels {%- endcomment -%}
      <div class="hero-tabs-content">
        {%- comment -%} Rules & Setup Tab {%- endcomment -%}
        {%- if section.settings.show_rules -%}
        <div class="hero-tab-panel active"
             id="hero-tab-rules-{{ section.id }}"
             role="tabpanel"
             aria-labelledby="hero-nav-rules-{{ section.id }}">

          <div class="hero-content-grid">
            <div class="content-main">
              <h3>{{ section.settings.rules_heading | default: 'Getting Started' }}</h3>
              <div class="content-text">
                <p>{{ section.settings.rules_content | default: 'Master the fundamentals of cosmic horror gaming with our comprehensive rules and setup guide.' }}</p>
              </div>

              {%- if section.settings.rules_video_url != blank -%}
                <div class="video-embed">
                  <iframe
                    src="{{ section.settings.rules_video_url }}"
                    title="Rules and Setup Video"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>
                  </iframe>
                </div>
              {%- endif -%}

              {%- if section.settings.rules_blog_link != blank -%}
                <div class="blog-content">
                  <h4>📖 Detailed Setup Guide</h4>
                  <p>{{ section.settings.rules_blog_excerpt | default: 'Read our comprehensive setup guide with tips from veteran players.' }}</p>
                  <a href="{{ section.settings.rules_blog_link }}" class="content-link">Read Full Guide →</a>
                </div>
              {%- endif -%}
            </div>

            <div class="content-sidebar">
              <div class="quick-links">
                <h4>Quick Links</h4>
                {%- if section.settings.rules_pdf_link != blank -%}
                  <a href="{{ section.settings.rules_pdf_link }}" class="quick-link" target="_blank">
                    <span class="link-icon">📄</span>
                    Download Rules PDF
                  </a>
                {%- endif -%}

                {%- if section.settings.rules_reference_link != blank -%}
                  <a href="{{ section.settings.rules_reference_link }}" class="quick-link" target="_blank">
                    <span class="link-icon">📋</span>
                    Quick Reference
                  </a>
                {%- endif -%}

                {%- if section.settings.rules_faq_link != blank -%}
                  <a href="{{ section.settings.rules_faq_link }}" class="quick-link">
                    <span class="link-icon">❓</span>
                    Frequently Asked Questions
                  </a>
                {%- endif -%}
              </div>

              {%- if product.metafields.petersen_games.game_stats.value.setup_time -%}
                <div class="game-info">
                  <h4>Setup Info</h4>
                  <div class="info-item">
                    <span class="info-label">Setup Time:</span>
                    <span class="info-value">{{ product.metafields.petersen_games.game_stats.value.setup_time }} min</span>
                  </div>
                </div>
              {%- endif -%}
            </div>
          </div>
        </div>
        {%- endif -%}

        {%- comment -%} Gameplay Tab {%- endcomment -%}
        {%- if section.settings.show_gameplay -%}
        <div class="hero-tab-panel"
             id="hero-tab-gameplay-{{ section.id }}"
             role="tabpanel"
             aria-labelledby="hero-nav-gameplay-{{ section.id }}">

          <div class="hero-content-grid">
            <div class="content-main">
              <h3>{{ section.settings.gameplay_heading | default: 'How to Play' }}</h3>
              <div class="content-text">
                <p>{{ section.settings.gameplay_content | default: 'Discover the strategic depth and cosmic horror mechanics that make this game unique.' }}</p>
              </div>

              {%- if section.settings.gameplay_video_url != blank -%}
                <div class="video-embed">
                  <iframe
                    src="{{ section.settings.gameplay_video_url }}"
                    title="Gameplay Overview Video"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>
                  </iframe>
                </div>
              {%- endif -%}

              {%- if section.settings.gameplay_blog_link != blank -%}
                <div class="blog-content">
                  <h4>🎲 Strategy Deep Dive</h4>
                  <p>{{ section.settings.gameplay_blog_excerpt | default: 'Learn advanced strategies and tactics from tournament players.' }}</p>
                  <a href="{{ section.settings.gameplay_blog_link }}" class="content-link">Read Strategy Guide →</a>
                </div>
              {%- endif -%}
            </div>

            <div class="content-sidebar">
              <div class="quick-links">
                <h4>Gameplay Resources</h4>
                {%- if section.settings.gameplay_overview_link != blank -%}
                  <a href="{{ section.settings.gameplay_overview_link }}" class="quick-link">
                    <span class="link-icon">🎯</span>
                    Gameplay Overview
                  </a>
                {%- endif -%}

                {%- if section.settings.strategy_guide_link != blank -%}
                  <a href="{{ section.settings.strategy_guide_link }}" class="quick-link">
                    <span class="link-icon">🧠</span>
                    Strategy Guide
                  </a>
                {%- endif -%}

                {%- if section.settings.gameplay_tips_link != blank -%}
                  <a href="{{ section.settings.gameplay_tips_link }}" class="quick-link">
                    <span class="link-icon">💡</span>
                    Pro Tips
                  </a>
                {%- endif -%}
              </div>

              {%- if product.metafields.petersen_games.game_stats.value -%}
                {%- assign game_stats = product.metafields.petersen_games.game_stats.value -%}
                <div class="game-info">
                  <h4>Game Stats</h4>
                  {%- if game_stats.play_time -%}
                    <div class="info-item">
                      <span class="info-label">Play Time:</span>
                      <span class="info-value">{{ game_stats.play_time.min }}-{{ game_stats.play_time.max }} min</span>
                    </div>
                  {%- endif -%}
                  {%- if game_stats.complexity -%}
                    <div class="info-item">
                      <span class="info-label">Complexity:</span>
                      <span class="info-value">{{ game_stats.complexity }}/5</span>
                    </div>
                  {%- endif -%}
                </div>
              {%- endif -%}
            </div>
          </div>
        </div>
        {%- endif -%}

        {%- comment -%} Downloads Tab {%- endcomment -%}
        {%- if section.settings.show_downloads -%}
        <div class="hero-tab-panel"
             id="hero-tab-downloads-{{ section.id }}"
             role="tabpanel"
             aria-labelledby="hero-nav-downloads-{{ section.id }}">

          <div class="hero-content-grid">
            <div class="content-main">
              <h3>{{ section.settings.downloads_heading | default: 'Downloads & Resources' }}</h3>
              <div class="content-text">
                <p>{{ section.settings.downloads_content | default: 'Access all the resources you need including rules, references, and bonus content.' }}</p>
              </div>

              <div class="downloads-grid">
                {%- if section.settings.download_1_title != blank -%}
                  <div class="download-item">
                    <div class="download-icon">📄</div>
                    <div class="download-info">
                      <h4>{{ section.settings.download_1_title }}</h4>
                      <p>{{ section.settings.download_1_description }}</p>
                      {%- if section.settings.download_1_url != blank -%}
                        <a href="{{ section.settings.download_1_url }}" class="download-button" target="_blank">Download</a>
                      {%- endif -%}
                    </div>
                  </div>
                {%- endif -%}

                {%- if section.settings.download_2_title != blank -%}
                  <div class="download-item">
                    <div class="download-icon">📋</div>
                    <div class="download-info">
                      <h4>{{ section.settings.download_2_title }}</h4>
                      <p>{{ section.settings.download_2_description }}</p>
                      {%- if section.settings.download_2_url != blank -%}
                        <a href="{{ section.settings.download_2_url }}" class="download-button" target="_blank">Download</a>
                      {%- endif -%}
                    </div>
                  </div>
                {%- endif -%}

                {%- if section.settings.download_3_title != blank -%}
                  <div class="download-item">
                    <div class="download-icon">🎲</div>
                    <div class="download-info">
                      <h4>{{ section.settings.download_3_title }}</h4>
                      <p>{{ section.settings.download_3_description }}</p>
                      {%- if section.settings.download_3_url != blank -%}
                        <a href="{{ section.settings.download_3_url }}" class="download-button" target="_blank">Download</a>
                      {%- endif -%}
                    </div>
                  </div>
                {%- endif -%}
              </div>

              {%- if section.settings.downloads_blog_link != blank -%}
                <div class="blog-content">
                  <h4>📚 Resource Library</h4>
                  <p>{{ section.settings.downloads_blog_excerpt | default: 'Explore our complete library of game resources and community content.' }}</p>
                  <a href="{{ section.settings.downloads_blog_link }}" class="content-link">Browse All Resources →</a>
                </div>
              {%- endif -%}
            </div>

            <div class="content-sidebar">
              <div class="quick-links">
                <h4>Popular Downloads</h4>
                {%- if section.settings.popular_1_title != blank -%}
                  <a href="{{ section.settings.popular_1_url }}" class="quick-link" target="_blank">
                    <span class="link-icon">🔥</span>
                    {{ section.settings.popular_1_title }}
                  </a>
                {%- endif -%}

                {%- if section.settings.popular_2_title != blank -%}
                  <a href="{{ section.settings.popular_2_url }}" class="quick-link" target="_blank">
                    <span class="link-icon">⭐</span>
                    {{ section.settings.popular_2_title }}
                  </a>
                {%- endif -%}

                {%- if section.settings.popular_3_title != blank -%}
                  <a href="{{ section.settings.popular_3_url }}" class="quick-link" target="_blank">
                    <span class="link-icon">💎</span>
                    {{ section.settings.popular_3_title }}
                  </a>
                {%- endif -%}
              </div>
            </div>
          </div>
        </div>
        {%- endif -%}

        {%- comment -%} Expansions Tab {%- endcomment -%}
        {%- if section.settings.show_expansions -%}
        <div class="hero-tab-panel"
             id="hero-tab-expansions-{{ section.id }}"
             role="tabpanel"
             aria-labelledby="hero-nav-expansions-{{ section.id }}">

          <div class="hero-content-grid">
            <div class="content-main">
              <h3>{{ section.settings.expansions_heading | default: 'Expand Your Universe' }}</h3>
              <div class="content-text">
                <p>{{ section.settings.expansions_content | default: 'Discover new realms, factions, and gameplay mechanics with our expansion collection.' }}</p>
              </div>

              {%- if section.settings.expansions_video_url != blank -%}
                <div class="video-embed">
                  <iframe
                    src="{{ section.settings.expansions_video_url }}"
                    title="Expansions Overview Video"
                    frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen>
                  </iframe>
                </div>
              {%- endif -%}

              {%- if product.metafields.petersen_games.expansions.value -%}
                {%- assign expansions = product.metafields.petersen_games.expansions.value -%}
                <div class="expansions-list">
                  {%- for expansion in expansions.available limit: 3 -%}
                    <div class="expansion-item">
                      <h4>{{ expansion.name }}</h4>
                      <p>{{ expansion.description }}</p>
                      {%- if expansion.available -%}
                        <span class="expansion-status available">Available Now</span>
                      {%- else -%}
                        <span class="expansion-status coming-soon">Coming Soon</span>
                      {%- endif -%}
                    </div>
                  {%- endfor -%}
                </div>
              {%- endif -%}

              {%- if section.settings.expansions_blog_link != blank -%}
                <div class="blog-content">
                  <h4>🌌 Expansion Deep Dive</h4>
                  <p>{{ section.settings.expansions_blog_excerpt | default: 'Learn about upcoming expansions and how they enhance gameplay.' }}</p>
                  <a href="{{ section.settings.expansions_blog_link }}" class="content-link">Read More →</a>
                </div>
              {%- endif -%}
            </div>

            <div class="content-sidebar">
              <div class="quick-links">
                <h4>Expansion Info</h4>
                {%- if section.settings.expansions_guide_link != blank -%}
                  <a href="{{ section.settings.expansions_guide_link }}" class="quick-link">
                    <span class="link-icon">📖</span>
                    Expansion Guide
                  </a>
                {%- endif -%}

                {%- if section.settings.compatibility_link != blank -%}
                  <a href="{{ section.settings.compatibility_link }}" class="quick-link">
                    <span class="link-icon">🔗</span>
                    Compatibility Chart
                  </a>
                {%- endif -%}

                {%- if section.settings.expansions_shop_link != blank -%}
                  <a href="{{ section.settings.expansions_shop_link }}" class="quick-link">
                    <span class="link-icon">🛒</span>
                    Shop Expansions
                  </a>
                {%- endif -%}
              </div>

              <div class="game-info">
                <h4>Quick Stats</h4>
                <div class="info-item">
                  <span class="info-label">Total Expansions:</span>
                  <span class="info-value">{{ section.settings.total_expansions | default: '5+' }}</span>
                </div>
                <div class="info-item">
                  <span class="info-label">Latest Release:</span>
                  <span class="info-value">{{ section.settings.latest_expansion | default: '2024' }}</span>
                </div>
              </div>
            </div>
          </div>
        </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const sectionId = '{{ section.id }}';
  const heroContainer = document.querySelector('#section-' + sectionId);

  if (heroContainer) {
    const tabButtons = heroContainer.querySelectorAll('.hero-tab-button');
    const tabPanels = heroContainer.querySelectorAll('.hero-tab-panel');

    tabButtons.forEach(button => {
      button.addEventListener('click', function() {
        const targetTab = this.dataset.tab;

        // Remove active states
        tabButtons.forEach(btn => {
          btn.classList.remove('active');
          btn.setAttribute('aria-selected', 'false');
        });

        tabPanels.forEach(panel => {
          panel.classList.remove('active');
        });

        // Add active state to clicked tab
        this.classList.add('active');
        this.setAttribute('aria-selected', 'true');

        // Show corresponding panel
        const targetPanel = heroContainer.querySelector('#hero-tab-' + targetTab + '-' + sectionId);
        if (targetPanel) {
          targetPanel.classList.add('active');
        }
      });

      // Keyboard navigation
      button.addEventListener('keydown', function(e) {
        if (e.key === 'Enter' || e.key === ' ') {
          e.preventDefault();
          this.click();
        }
      });
    });
  }
});
</script>

<style>
.petersen-core-hero {
  margin: {{ section.settings.margin_top }}px 0 {{ section.settings.margin_bottom }}px 0;
  padding: 0 20px;
  background: transparent;
}

.hero-container {
  max-width: 1200px;
  margin: 0 auto;
  color: white;
  font-family: 'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif;
}

.hero-header {
  text-align: center;
  margin-bottom: 40px;
}

.hero-title {
  font-size: 2rem;
  font-weight: 700;
  color: white;
  margin: 0 0 16px 0;
  letter-spacing: -0.02em;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  text-shadow: 0 2px 12px rgba(0, 0, 0, 0.7), 0 0 25px rgba(255, 255, 255, 0.12);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

.hero-subtitle {
  color: rgba(255, 255, 255, 0.9);
  font-size: 18px;
  margin: 0;
  line-height: 1.5;
  text-shadow: 0 1px 6px rgba(0, 0, 0, 0.5), 0 0 15px rgba(255, 255, 255, 0.08);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-weight: 400;
}

.hero-tabs-wrapper {
  background: rgba(81, 47, 235, 0.08);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(81, 47, 235, 0.15);
  border-radius: 20px;
  overflow: hidden;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.1),
    0 8px 32px rgba(81, 47, 235, 0.15),
    0 0 0 1px rgba(81, 47, 235, 0.05);
}

/* Apple Store-style Tab Navigation with glass container */
.hero-tabs-nav {
  display: flex;
  background: rgba(81, 47, 235, 0.05);
  backdrop-filter: blur(8px);
  border: 1px solid rgba(81, 47, 235, 0.08);
  border-radius: 12px;
  padding: 4px;
  gap: 4px;
  margin-bottom: 24px;
  overflow-x: auto;
  scrollbar-width: none;
  -ms-overflow-style: none;
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.05),
    0 2px 8px rgba(81, 47, 235, 0.1);
}

.hero-tabs-nav::-webkit-scrollbar {
  display: none;
}

.hero-tab-button {
  background: transparent;
  border: none;
  color: rgba(255, 255, 255, 0.8);
  cursor: pointer;
  transition: all 0.2s cubic-bezier(0.2, 0, 0, 1);
  padding: 12px 20px;
  display: flex;
  align-items: center;
  gap: 8px;
  flex: 1;
  min-width: 120px;
  font-family: inherit;
  border-radius: 8px;
  font-size: {{ section.settings.tab_font_size | default: '14px' }};
  font-weight: {{ section.settings.tab_font_weight | default: '500' }};
  text-align: center;
  justify-content: center;
  white-space: nowrap;
  /* Apple-style touch optimization */
  -webkit-tap-highlight-color: transparent;
  touch-action: manipulation;
}

.hero-tab-button:hover {
  color: white;
  background-color: rgba(81, 47, 235, 0.15);
  backdrop-filter: blur(8px);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.1),
    0 2px 8px rgba(81, 47, 235, 0.2),
    0 0 0 1px rgba(81, 47, 235, 0.1);
  transform: translateY(-0.5px);
}

.hero-tab-button.active {
  color: rgba(255, 255, 255, 1);
  background-color: rgba(81, 47, 235, 0.9);
  backdrop-filter: blur(12px);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.2),
    0 6px 20px rgba(81, 47, 235, 0.4),
    0 2px 8px rgba(0, 0, 0, 0.15),
    0 0 0 1px rgba(81, 47, 235, 0.2);
  font-weight: 600;
  transform: translateY(-1px) scale(1.02);
}

.hero-tab-button.active:hover {
  background-color: rgba(81, 47, 235, 1);
  box-shadow:
    inset 0 1px 0 rgba(255, 255, 255, 0.25),
    0 8px 25px rgba(81, 47, 235, 0.5),
    0 3px 10px rgba(0, 0, 0, 0.2),
    0 0 0 1px rgba(81, 47, 235, 0.3);
  transform: translateY(-1px) scale(1.02);
}

.hero-tab-button:focus {
  outline: 2px solid rgba(102, 126, 234, 0.5);
  outline-offset: 2px;
}

.tab-glyph {
  width: 16px;
  height: 16px;
  opacity: 0.8;
  transition: all 0.2s ease;
  flex-shrink: 0;
}

.hero-tab-button:hover .tab-glyph {
  opacity: 1;
}

.hero-tab-button.active .tab-glyph {
  opacity: 1;
}

.tab-label {
  font-size: inherit;
  font-weight: inherit;
  text-align: center;
  letter-spacing: -0.01em;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Hero Tab Content */
.hero-tabs-content {
  padding: 40px;
  min-height: 500px;
}

.hero-tab-panel {
  display: none;
  animation: heroFadeIn 0.4s ease-in-out;
}

.hero-tab-panel.active {
  display: block;
}

@keyframes heroFadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.hero-content-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  gap: 40px;
  align-items: start;
}

.content-main h3 {
  font-size: 1.5rem;
  font-weight: 700;
  color: white;
  margin: 0 0 20px 0;
}

.content-text p {
  font-size: 16px;
  line-height: 1.6;
  color: rgba(255, 255, 255, 0.95);
  margin: 0 0 24px 0;
  text-shadow: 0 1px 4px rgba(0, 0, 0, 0.4), 0 0 10px rgba(255, 255, 255, 0.06);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-weight: 400;
}

.video-embed {
  position: relative;
  padding-bottom: 56.25%;
  height: 0;
  overflow: hidden;
  border-radius: 12px;
  margin: 24px 0;
  background: rgba(0, 0, 0, 0.3);
}

.video-embed iframe {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  border-radius: 12px;
}

.blog-content {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 24px;
  margin: 24px 0;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.blog-content h4 {
  font-size: 18px;
  font-weight: 600;
  color: white;
  margin: 0 0 12px 0;
}

.blog-content p {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.9);
  line-height: 1.5;
  margin: 0 0 16px 0;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3), 0 0 8px rgba(255, 255, 255, 0.05);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-weight: 400;
}

.content-link {
  color: #667eea;
  text-decoration: none;
  font-weight: 500;
  transition: color 0.2s ease;
}

.content-link:hover {
  color: #5a67d8;
  text-decoration: underline;
}

.content-sidebar {
  position: sticky;
  top: 20px;
}

.quick-links {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 24px;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.quick-links h4 {
  font-size: 16px;
  font-weight: 600;
  color: white;
  margin: 0 0 16px 0;
}

.quick-link {
  display: flex;
  align-items: center;
  gap: 12px;
  color: rgba(255, 255, 255, 0.8);
  text-decoration: none;
  padding: 12px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  font-size: 14px;
  transition: color 0.2s ease;
}

.quick-link:last-child {
  border-bottom: none;
}

.quick-link:hover {
  color: #667eea;
}

.link-icon {
  font-size: 16px;
  width: 20px;
  flex-shrink: 0;
}

.game-info {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 24px;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.game-info h4 {
  font-size: 16px;
  font-weight: 600;
  color: white;
  margin: 0 0 16px 0;
}

.info-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 8px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.info-item:last-child {
  border-bottom: none;
}

.info-label {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.7);
}

.info-value {
  font-size: 14px;
  font-weight: 600;
  color: #667eea;
  text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3), 0 0 8px rgba(102, 126, 234, 0.2);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Downloads specific styles */
.downloads-grid {
  display: grid;
  gap: 20px;
  margin: 24px 0;
}

.download-item {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 24px;
  border: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  align-items: flex-start;
  gap: 20px;
  transition: all 0.2s ease;
}

.download-item:hover {
  background: rgba(255, 255, 255, 0.08);
  transform: translateY(-2px);
}

.download-icon {
  font-size: 24px;
  width: 48px;
  height: 48px;
  background: rgba(102, 126, 234, 0.1);
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.download-info {
  flex: 1;
}

.download-info h4 {
  font-size: 16px;
  font-weight: 600;
  color: white;
  margin: 0 0 8px 0;
}

.download-info p {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.7);
  margin: 0 0 12px 0;
  line-height: 1.4;
}

.download-button {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: white;
  text-decoration: none;
  padding: 8px 16px;
  border-radius: 6px;
  font-size: 12px;
  font-weight: 600;
  transition: all 0.2s ease;
  display: inline-block;
}

.download-button:hover {
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
}

/* Expansions specific styles */
.expansions-list {
  display: grid;
  gap: 20px;
  margin: 24px 0;
}

.expansion-item {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  padding: 24px;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.expansion-item h4 {
  font-size: 16px;
  font-weight: 600;
  color: white;
  margin: 0 0 8px 0;
}

.expansion-item p {
  font-size: 14px;
  color: rgba(255, 255, 255, 0.7);
  margin: 0 0 12px 0;
  line-height: 1.4;
}

.expansion-status {
  font-size: 12px;
  font-weight: 600;
  padding: 4px 8px;
  border-radius: 4px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.expansion-status.available {
  background: rgba(34, 197, 94, 0.1);
  color: #22c55e;
  border: 1px solid rgba(34, 197, 94, 0.2);
}

.expansion-status.coming-soon {
  background: rgba(245, 158, 11, 0.1);
  color: #f59e0b;
  border: 1px solid rgba(245, 158, 11, 0.2);
}

/* Mobile Responsiveness */
@media (max-width: 968px) {
  .hero-content-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }

  .content-sidebar {
    position: relative;
    top: auto;
  }

  .hero-tabs-content {
    padding: 24px;
  }
}

@media (max-width: 768px) {
  .petersen-core-hero {
    margin: {{ section.settings.margin_top }}px 0 {{ section.settings.margin_bottom }}px 0;
    padding: 0 16px; /* Reduce side padding */
  }

  .hero-container {
    padding: 24px; /* Reduce overall padding */
  }

  .hero-header {
    margin-bottom: 24px; /* Tighten spacing */
  }

  .hero-title {
    font-size: {{ section.settings.mobile_title_size | default: '1.5rem' }};
  }

  .hero-subtitle {
    font-size: {{ section.settings.mobile_subtitle_size | default: '16px' }};
  }

  .hero-tabs-nav {
    padding: 3px; /* Reduce tab container padding */
    gap: 3px;
    margin-bottom: 16px; /* Reduce bottom margin */
  }

  .hero-tab-button {
    padding: 10px 16px; /* Reduce tab padding */
    min-width: 100px; /* Smaller minimum width */
    font-size: {{ section.settings.mobile_tab_font_size | default: '13px' }};
    border-radius: 6px; /* Smaller border radius */
  }

  .tab-glyph {
    width: 14px; /* Smaller icons */
    height: 14px;
  }

  .hero-tabs-content {
    padding: 20px; /* Reduce content padding */
    min-height: 300px; /* Reduce minimum height */
  }

  .content-main h3 {
    font-size: {{ section.settings.mobile_heading_size | default: '1.25rem' }};
    margin-bottom: 12px; /* Tighten spacing */
  }

  .content-text p {
    font-size: {{ section.settings.mobile_content_size | default: '14px' }};
    line-height: 1.5; /* Tighter line height */
    margin-bottom: 16px;
  }
}

@media (max-width: 480px) {
  .petersen-core-hero {
    padding: 0 12px; /* Even tighter mobile padding */
  }

  .hero-container {
    padding: 20px;
  }

  .hero-tabs-nav {
    flex-direction: column; /* Stack vertically for very small screens */
    gap: 2px;
  }

  .hero-tab-button {
    flex-direction: row; /* Horizontal layout for compact design */
    justify-content: flex-start; /* Left align as requested */
    text-align: left;
    padding: 12px 16px;
    gap: 12px;
    min-width: auto;
    border-radius: 8px;
  }

  .tab-label {
    text-align: left; /* Left align text */
  }

  .downloads-grid,
  .expansions-list {
    gap: 12px;
  }

  .download-item,
  .expansion-item {
    padding: 12px;
  }
}
</style>

{% schema %}
{
  "name": "Core Game Hero",
  "tag": "section",
  "class": "petersen-core-hero-section",
  "settings": [
    {
      "type": "header",
      "content": "Hero Header"
    },
    {
      "type": "checkbox",
      "id": "show_title",
      "label": "Show hero title",
      "default": true
    },
    {
      "type": "text",
      "id": "title",
      "label": "Hero title",
      "default": "Explore the Game Universe"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Hero subtitle (optional)",
      "default": "Dive deeper into the cosmic horror experience with comprehensive guides, resources, and expansions."
    },
    {
      "type": "header",
      "content": "Tab Icon Settings"
    },
    {
      "type": "image_picker",
      "id": "gameplay_icon",
      "label": "Gameplay tab icon",
      "info": "Upload a custom icon for the gameplay tab"
    },
    {
      "type": "image_picker",
      "id": "downloads_icon",
      "label": "Downloads tab icon",
      "info": "Upload a custom icon for the downloads tab"
    },
    {
      "type": "image_picker",
      "id": "expansions_icon",
      "label": "Expansions tab icon",
      "info": "Upload a custom icon for the expansions tab"
    },
    {
      "type": "range",
      "id": "tab_icon_width",
      "min": 12,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Tab icon width",
      "default": 16,
      "info": "Controls the size of tab icons (height will match width)"
    },
    {
      "type": "header",
      "content": "Typography Controls"
    },
    {
      "type": "select",
      "id": "tab_font_size",
      "label": "Desktop tab font size",
      "options": [
        {"value": "12px", "label": "Small (12px)"},
        {"value": "13px", "label": "Small-Medium (13px)"},
        {"value": "14px", "label": "Medium (14px)"},
        {"value": "15px", "label": "Medium-Large (15px)"},
        {"value": "16px", "label": "Large (16px)"}
      ],
      "default": "14px"
    },
    {
      "type": "select",
      "id": "tab_font_weight",
      "label": "Tab font weight",
      "options": [
        {"value": "400", "label": "Regular"},
        {"value": "500", "label": "Medium"},
        {"value": "600", "label": "Semi-Bold"},
        {"value": "700", "label": "Bold"}
      ],
      "default": "500"
    },
    {
      "type": "select",
      "id": "mobile_tab_font_size",
      "label": "Mobile tab font size",
      "options": [
        {"value": "11px", "label": "Extra Small (11px)"},
        {"value": "12px", "label": "Small (12px)"},
        {"value": "13px", "label": "Medium (13px)"},
        {"value": "14px", "label": "Large (14px)"}
      ],
      "default": "13px"
    },
    {
      "type": "select",
      "id": "mobile_title_size",
      "label": "Mobile title size",
      "options": [
        {"value": "1.25rem", "label": "Small (1.25rem)"},
        {"value": "1.5rem", "label": "Medium (1.5rem)"},
        {"value": "1.75rem", "label": "Large (1.75rem)"}
      ],
      "default": "1.5rem"
    },
    {
      "type": "select",
      "id": "mobile_content_size",
      "label": "Mobile content font size",
      "options": [
        {"value": "13px", "label": "Small (13px)"},
        {"value": "14px", "label": "Medium (14px)"},
        {"value": "15px", "label": "Large (15px)"},
        {"value": "16px", "label": "Extra Large (16px)"}
      ],
      "default": "14px"
    },
    {
      "type": "header",
      "content": "Tab Content Settings"
    },
    {
      "type": "checkbox",
      "id": "show_rules",
      "label": "Show Rules & Setup tab",
      "default": true
    },
    {
      "type": "text",
      "id": "rules_title",
      "label": "Rules tab title",
      "default": "Rules & Setup"
    },
    {
      "type": "text",
      "id": "rules_heading",
      "label": "Rules section heading",
      "default": "Getting Started"
    },
    {
      "type": "textarea",
      "id": "rules_content",
      "label": "Rules content",
      "default": "Master the fundamentals of cosmic horror gaming with our comprehensive rules and setup guide."
    },
    {
      "type": "url",
      "id": "rules_video_url",
      "label": "Rules video embed URL (optional)",
      "info": "YouTube, Vimeo, or other video platform embed URL"
    },
    {
      "type": "url",
      "id": "rules_blog_link",
      "label": "Rules blog post link (optional)"
    },
    {
      "type": "textarea",
      "id": "rules_blog_excerpt",
      "label": "Rules blog excerpt",
      "default": "Read our comprehensive setup guide with tips from veteran players."
    },
    {
      "type": "url",
      "id": "rules_pdf_link",
      "label": "Rules PDF download link (optional)"
    },
    {
      "type": "url",
      "id": "rules_reference_link",
      "label": "Quick reference link (optional)"
    },
    {
      "type": "url",
      "id": "rules_faq_link",
      "label": "FAQ link (optional)"
    },
    {
      "type": "header",
      "content": "Gameplay Tab"
    },
    {
      "type": "checkbox",
      "id": "show_gameplay",
      "label": "Show Gameplay tab",
      "default": true
    },
    {
      "type": "text",
      "id": "gameplay_title",
      "label": "Gameplay tab title",
      "default": "Gameplay"
    },
    {
      "type": "text",
      "id": "gameplay_heading",
      "label": "Gameplay section heading",
      "default": "How to Play"
    },
    {
      "type": "textarea",
      "id": "gameplay_content",
      "label": "Gameplay content",
      "default": "Discover the strategic depth and cosmic horror mechanics that make this game unique."
    },
    {
      "type": "url",
      "id": "gameplay_video_url",
      "label": "Gameplay video embed URL (optional)"
    },
    {
      "type": "url",
      "id": "gameplay_blog_link",
      "label": "Gameplay blog post link (optional)"
    },
    {
      "type": "textarea",
      "id": "gameplay_blog_excerpt",
      "label": "Gameplay blog excerpt",
      "default": "Learn advanced strategies and tactics from tournament players."
    },
    {
      "type": "url",
      "id": "gameplay_overview_link",
      "label": "Gameplay overview link (optional)"
    },
    {
      "type": "url",
      "id": "strategy_guide_link",
      "label": "Strategy guide link (optional)"
    },
    {
      "type": "url",
      "id": "gameplay_tips_link",
      "label": "Pro tips link (optional)"
    },
    {
      "type": "header",
      "content": "Downloads Tab"
    },
    {
      "type": "checkbox",
      "id": "show_downloads",
      "label": "Show Downloads tab",
      "default": true
    },
    {
      "type": "text",
      "id": "downloads_title",
      "label": "Downloads tab title",
      "default": "Downloads"
    },
    {
      "type": "text",
      "id": "downloads_heading",
      "label": "Downloads section heading",
      "default": "Downloads & Resources"
    },
    {
      "type": "textarea",
      "id": "downloads_content",
      "label": "Downloads content",
      "default": "Access all the resources you need including rules, references, and bonus content."
    },
    {
      "type": "text",
      "id": "download_1_title",
      "label": "Download 1 title",
      "default": "Official Rulebook"
    },
    {
      "type": "textarea",
      "id": "download_1_description",
      "label": "Download 1 description",
      "default": "Complete rules and setup instructions in PDF format."
    },
    {
      "type": "url",
      "id": "download_1_url",
      "label": "Download 1 URL"
    },
    {
      "type": "text",
      "id": "download_2_title",
      "label": "Download 2 title",
      "default": "Quick Reference"
    },
    {
      "type": "textarea",
      "id": "download_2_description",
      "label": "Download 2 description",
      "default": "Handy reference card for gameplay mechanics."
    },
    {
      "type": "url",
      "id": "download_2_url",
      "label": "Download 2 URL"
    },
    {
      "type": "text",
      "id": "download_3_title",
      "label": "Download 3 title",
      "default": "Bonus Content"
    },
    {
      "type": "textarea",
      "id": "download_3_description",
      "label": "Download 3 description",
      "default": "Additional scenarios and game variants."
    },
    {
      "type": "url",
      "id": "download_3_url",
      "label": "Download 3 URL"
    },
    {
      "type": "url",
      "id": "downloads_blog_link",
      "label": "Downloads blog post link (optional)"
    },
    {
      "type": "textarea",
      "id": "downloads_blog_excerpt",
      "label": "Downloads blog excerpt",
      "default": "Explore our complete library of game resources and community content."
    },
    {
      "type": "text",
      "id": "popular_1_title",
      "label": "Popular download 1 title",
      "default": "Player's Guide"
    },
    {
      "type": "url",
      "id": "popular_1_url",
      "label": "Popular download 1 URL"
    },
    {
      "type": "text",
      "id": "popular_2_title",
      "label": "Popular download 2 title",
      "default": "Strategy Cards"
    },
    {
      "type": "url",
      "id": "popular_2_url",
      "label": "Popular download 2 URL"
    },
    {
      "type": "text",
      "id": "popular_3_title",
      "label": "Popular download 3 title",
      "default": "Scenario Pack"
    },
    {
      "type": "url",
      "id": "popular_3_url",
      "label": "Popular download 3 URL"
    },
    {
      "type": "header",
      "content": "Expansions Tab"
    },
    {
      "type": "checkbox",
      "id": "show_expansions",
      "label": "Show Expansions tab",
      "default": true
    },
    {
      "type": "text",
      "id": "expansions_title",
      "label": "Expansions tab title",
      "default": "Expansions"
    },
    {
      "type": "text",
      "id": "expansions_heading",
      "label": "Expansions section heading",
      "default": "Expand Your Universe"
    },
    {
      "type": "textarea",
      "id": "expansions_content",
      "label": "Expansions content",
      "default": "Discover new realms, factions, and gameplay mechanics with our expansion collection."
    },
    {
      "type": "url",
      "id": "expansions_video_url",
      "label": "Expansions video embed URL (optional)"
    },
    {
      "type": "url",
      "id": "expansions_blog_link",
      "label": "Expansions blog post link (optional)"
    },
    {
      "type": "textarea",
      "id": "expansions_blog_excerpt",
      "label": "Expansions blog excerpt",
      "default": "Learn about upcoming expansions and how they enhance gameplay."
    },
    {
      "type": "url",
      "id": "expansions_guide_link",
      "label": "Expansion guide link (optional)"
    },
    {
      "type": "url",
      "id": "compatibility_link",
      "label": "Compatibility chart link (optional)"
    },
    {
      "type": "url",
      "id": "expansions_shop_link",
      "label": "Shop expansions link (optional)"
    },
    {
      "type": "text",
      "id": "total_expansions",
      "label": "Total expansions count",
      "default": "5+"
    },
    {
      "type": "text",
      "id": "latest_expansion",
      "label": "Latest expansion year",
      "default": "2024"
    },
    {
      "type": "header",
      "content": "Layout Settings"
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Top margin",
      "default": 60
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "px",
      "label": "Bottom margin",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "Core Game Hero",
      "settings": {
        "show_title": true,
        "title": "Explore the Game Universe",
        "show_rules": true,
        "show_gameplay": true,
        "show_downloads": true,
        "show_expansions": true
      }
    }
  ]
}
{% endschema %}